import http from 'node:http';
import assert from 'node:assert/strict';
import {runLiveScan} from '../collector/live-runtime.mjs';
const server=http.createServer((req,res)=>{const n=Number((req.url.match(/s(\d+)/)||[])[1]||1);const events=[1,2].map(i=>({'@type':'Event',name:`Mock event ${n}-${i}`,startDate:`2026-08-${20+n}T1${i}:00:00+02:00`,location:{'@type':'Place',name:`Venue ${n}`,address:{addressLocality:'Bruxelles'}},url:`http://127.0.0.1:${server.address().port}/event/${n}/${i}`}));res.writeHead(200,{'content-type':'text/html'});res.end(`<script type="application/ld+json">${JSON.stringify(events)}</script>`);});
await new Promise(r=>server.listen(0,'127.0.0.1',r));const port=server.address().port;const sources=Array.from({length:4},(_,i)=>({id:`mock-${i+1}`,name:`Mock ${i+1}`,url:`http://127.0.0.1:${port}/s${i+1}`,format:'jsonld',access:'direct',city:'Bruxelles'}));const ok=await runLiveScan(null,{sources,concurrency:4,mode:'qa'});assert.equal(ok.accepted,true);assert.equal(ok.payload.stats.fresh,true);assert.equal(ok.payload.stats.productiveSources,4);assert.ok(ok.payload.events.length>=8);server.close();const dead=Array.from({length:4},(_,i)=>({id:`dead-${i+1}`,name:`Dead ${i+1}`,url:'http://127.0.0.1:9/nope',format:'jsonld',access:'direct',city:'Bruxelles'}));const fail=await runLiveScan(null,{sources:dead,concurrency:4,mode:'qa-fail'});assert.equal(fail.accepted,false);assert.equal(fail.payload.events.length,0);assert.equal(fail.payload.generatedAt,null);assert.equal(fail.payload.stats.dataOrigin,'live-scan-failed');console.log(JSON.stringify({ok:{events:ok.payload.events.length,productive:ok.payload.stats.productiveSources},fail:{events:fail.payload.events.length,origin:fail.payload.stats.dataOrigin}},null,2));
